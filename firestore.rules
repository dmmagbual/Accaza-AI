rules_version = '2';
// Every read and write goes through the Cloud Functions (Admin SDK), which enforce login,
// the owner/staff/member/guest tiers and the daily limits. Browsers get no direct access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
